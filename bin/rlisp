#! /usr/bin/env node

const fs = require('fs')
const { readAll } = require('../reader')
const { evalForms } = require('../eval')

const args = process.argv.slice(2)
const [path] = args

const input = fs.readFileSync(path).toString()

const forms = readAll(input)

evalForms(forms)

